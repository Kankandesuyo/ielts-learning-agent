# IELTS Learning Agent 项目报告

更新时间：2026-07-19

## 一、产品定位

项目面向缺少明确备考路径的 IELTS 初学者。主管 Agent 读取画像和历史错题，选择下一项训练；专项模块负责听、说、读、写、词汇和本地资料训练。

当前定位是“可运行的本地学习 Agent MVP”，不是官方 IELTS 评分系统，也暂不适合直接部署为公开多用户服务。

## 二、本轮完成内容

1. 建立 Git 仓库和 `.gitignore`，密钥、数据库、上传文件、知识索引、教材原文件和临时输出默认不进入版本控制。
2. 新增画像读取、修改、删除 API；删除画像时同步清理其计划、错题、词汇、资料记录和上传文件。
3. 新增学习计划持久化：`study_plans` 保存计划，`study_plan_days` 保存每日任务和完成状态。
4. 前端支持读取最近计划、完成/撤销每日打卡，并实时显示完成天数和进度百分比。
5. 前端统一转义动态内容，覆盖模型输出、作文、错题、资料来源、文件名和备注，降低存储型 XSS 风险。
6. 上传安全增加文件名、分类、备注、大小、扩展名和文件头校验；服务端返回可信 MIME 类型。
7. 输入模型增加用户 ID、技能名称、任务类型和文本长度边界。
8. 自动化测试切换为临时数据库和临时上传目录，避免污染正式 `ielts_agent.db`。
9. 保留并纳入现有本地资料出题模块：PDF/TXT/MD 索引、来源书名/页码、题目生成和答案分析。
10. 将学生前端从 12 个并列功能入口收敛为 3 个主入口：今日学习、专项训练、资料与记录。
11. 画像、主管调度和学习计划组成同一条“今日学习”流程；听说读写词汇以及模考、资料、错题改为页内切换，后端功能和接口均保留。
12. 从学生导航移除 API Docs 技术入口，降低初学者认知负担；旧的页面哈希仍映射到对应新工作区，避免已有链接失效。
13. 模拟考试新增题目词汇助手：学生用左键拖选单词或词组，松开后立即黄色标亮并自动分析；双击单词同样自动分析，不再需要二次点击解释按钮。
14. 词汇解释包含词性、中文释义、当前题目语境、简单英文解释、记忆提示、例句以及来源书名和页码。
15. 后端不信任浏览器提供的上下文，只接收所选词和试卷分区，再从 `database` 中的 Cambridge PDF 重新定位原题；不存在的词会被拒绝。
16. 词汇助手已真正接入 `database/legal-dictionaries`：优先解析 FreeDict StarDict 英中词典，未命中时查询 GCIDE 英英词典，并支持常见英文词形回退。整个本地命中流程不联网，也不传输画像、作答或密钥。
17. 阅读考试改为桌面双栏：问题和答案输入框在左，阅读原文在右并可独立滚动；手机端自动回退为问题在前、原文在后的单栏。
18. 手机端词汇解释改为最高占屏 65% 的底部抽屉；加载中也始终显示“关闭”，支持 Esc 退出，关闭后会作废进行中的请求，避免迟到结果重新弹出。
19. 英文选择范围扩展到整份试卷：题目、选项和 Reading Passage 中的英文均可拖选或双击标亮并解释；后端根据所选区域回查 PDF 对应题目页或原文页。
19. 本地词典未命中时，才继续使用已配置 AI 模型；模型不可用时再按配置降级到公开词典与翻译服务。界面会明确显示实际解释来源。
20. 网页的后端模型供应商已切换到 DeepSeek 官方 OpenAI-compatible 接口，当前型号为 `deepseek-v4-flash`；真实密钥只保存在被 Git 忽略的 `.env`，不会进入浏览器 JavaScript。

## 三、验证证据

- `.venv\\Scripts\\python.exe -m pytest --cov=app --cov-report=term-missing -q`：`30 passed`，后端行覆盖率 `90%`（补测前为 `82%`）。
- `node --check app/static/app.js`：通过。
- `node tests/frontend_security_test.js`：动态内容 XSS 转义检查通过。
- 真实后端接口查询 `contradicts` 返回 HTTP 200、`provider=local_dictionary`、`dictionary_source=GNU GCIDE 0.54`，没有依赖公网 API。
- FreeDict StarDict 解析测试查询 `apple`，取得词性 `noun` 和中文释义“苹果/蘋果”。
- DeepSeek 真实最小调用：`enabled=true`、官方 Base URL 匹配、型号 `deepseek-v4-flash`、返回 `status=ok`；验证过程未输出密钥或模型正文。
- 应用内浏览器真实验收：
  - 桌面端只显示 3 个主导航入口。
  - `专项训练 -> 口语` 页内切换成功。
  - `资料与记录 -> 错题本` 页内切换成功。
  - 390px 手机视口下 `scrollWidth` 与 `clientWidth` 相等，无横向溢出。
  - 在 Cambridge 16 Test 1 题目中左键拖选 `unintelligent`，松开后未点击按钮即自动标亮并返回解释。
  - 真实返回 `verb`、中文释义、英文定义、例句及 PDF 第 18、19 页来源。
  - 解释侧边面板可收起；收起后题目中的标亮仍保留。
  - 手机端解释使用底部抽屉，不再占满全屏；加载中可关闭，关闭后的迟到请求不会重新打开抽屉。
  - 390×844 视口实测：加载抽屉高约 151px，长结果最高 65vh；“关闭”和 Esc 均可退出，等待 5 秒仍保持关闭，控制台无错误或警告。
  - 英文选择范围实测：题目中的 `their` 与 Reading Passage 中的 `comparative` 均能双击标亮并解释；原文词来源回查到 PDF 第 16、17 页，桌面和 390px 手机端均无横向溢出。
  - 词汇助手桌面端与 390px 手机视口均无横向溢出，浏览器控制台无错误或警告。
  - 桌面端实测问题栏横坐标小于原文栏，双栏宽度约为 391px / 433px；390px 手机视口恢复为单列且问题位于原文之前。
- 真实 Uvicorn HTTP 流程：
  - 首页返回 200，页面标题存在。
  - 创建画像返回 200。
  - 生成 2 天学习计划返回计划 ID。
  - Day 1 打卡后 `completed_days=1`、`progress_percent=50`。
  - 读取最近计划返回相同计划 ID。
- 边界测试覆盖：目标分低于当前分、非法技能、未知用户、计划/日期不存在、完成状态撤销、跨用户文件访问、非法扩展名/分类/备注/二进制文本/空文件、模型关闭与网络失败安全降级、资料题所有权、画像 CRUD、计划打卡、资料上传下载删除。
- 学习模块覆盖：阅读、听力、词汇、写作、口语、主管 Agent、学习计划、资料库出题、阅读考试、前端入口与 XSS 转义均进入自动化测试；阅读、听力、词汇和学习计划路由达到 `100%` 行覆盖。

## 四、当前安全边界

已经具备输入校验、文件校验、密钥隔离、前端输出转义和测试数据隔离。

尚未具备登录鉴权。当前接口仍依赖可猜测的数字 `user_id`，因此只能作为本地单用户系统使用。公开部署前的 P0 工作是登录/会话、服务端所有权校验、限流、HTTPS、日志脱敏和数据库迁移。

## 五、仍需完成

1. 真实 LLM 目前主要生成主管建议，写作与口语评分仍以规则逻辑为主。
2. 用户上传的 DOCX/PPTX/PDF 还没有自动解析进个人 RAG；本地 `database` 资料索引是另一条资料链路。
3. 缺少正式账号、密码重置、角色和多用户数据隔离。
4. 缺少周/月进步趋势、连续学习天数、提醒和动态计划调整。
5. 缺少真实口语录音、语音识别、发音分析和听力音频播放器。

## 六、下一阶段优先级

P0：登录鉴权与数据所有权隔离。

P1：让真实 LLM 使用评分标准和检索上下文完成结构化写作/口语反馈。

P1：增加学习统计仪表盘和计划动态调整。

P2：音频训练、PostgreSQL、Docker 和生产部署。
